import Layout from "../components/MyLayout.js";

const pageStyle = {
  margin: process.env.DEFAULT_PAGE_MARGINS
};

const Integrations = () => (
  <Layout>
    <div style={pageStyle}>
      <h1>Integrations Page - Under Construction.</h1>
    </div>
  </Layout>
);

export default Integrations;
